enum ParticipantGoalErrorCode {
  PARTICIPANT_GOAL_NOT_FOUND = "Participant goal not found!",
  PARTICIPANT_GOAL_ALREADY_EXISTS = "Participant goal already exists",
}

export default ParticipantGoalErrorCode;
